# Local-only placeholders for `pnpm dev` visual testing.
# Copy this file to apps/web/.env.local — never commit a populated .env.local.
# These values are dummies; auth + events flows will fail at click time but the
# pages will render so you can preview layouts.

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.dummy.dummy
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.dummy.dummy
UPSTASH_REDIS_REST_URL=http://127.0.0.1:8079
UPSTASH_REDIS_REST_TOKEN=dummy-redis-token
